{
  "name": "loom-workspace",
  "description": "Loom-managed workspace with AI development orchestration",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "test": "python3 -m unittest discover -s sim/tests -t sim/tests && python3 -m unittest discover -s layout/tests -t layout/tests",
    "check:ci": "bash sim/selftest.sh"
  }
}
